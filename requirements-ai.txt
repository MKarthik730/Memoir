# Optional semantic-search dependency (~500MB with PyTorch).
# Search works fully on keyword scoring without this — installing it just
# adds semantic (meaning-based) matching on top. No database extension or
# background worker required; similarity is computed in Python.
# pip install -r requirements-ai.txt

sentence-transformers==2.7.0
